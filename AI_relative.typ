#set heading(numbering:"1.1")
#heading(numbering:none)[AI知识点碎片积累]

= Token（标记/语元）
  == 定义（学理）： 
  Token 是大型语言模型（LLM）处理和生成文本时的最小离散语义单位。它不是字符（如汉字），也不是单纯的单词（如英文单词），而是通过特定的*分词算法（Tokenization）*得到的片段。

  == 技术原理： 
  分词器（如 BPE, SentencePiece）将文本序列切分为更小的子词（Subwords）。例如，"unhappiness" 可能被切分为 ["un", "happi", "ness"]。每个 Token 会被分配一个唯一的 ID，并映射到一个稠密的向量空间（Embedding），作为模型的输入。

  == 功能： 
  Token 是 LLM 计算复杂度的量度单位。它直接影响模型的推理成本（Token 计数）、上下文窗口（模型能记忆的 Token 上限）以及生成文本时的概率选择。

= LLM (Large Language Model，大型语言模型)

  == 定义（学理）： 
  LLM 是基于深度学习（特别是 Transformer 架构）的大规模参数神经网络模型。它通过在海量多语种文本数据上进行自监督预训练，学习到通用的语言统计规律、逻辑、甚至世界知识。

  == 技术原理： 
  核心是自注意力机制（Self-Attention），它允许模型在处理一个序列时，同时“关注”序列中的所有其他位置，从而捕捉长距离依赖关系。预训练阶段（Pre-training）通过最大化下一个 Token 的预测概率来学习。

  == 功能： 
  它是系统的“思考核心”，具有强大的语言理解、文本生成、翻译、摘要、逻辑推理和少样本/零样本学习能力。它本质上是一个根据给定上下文生成 Token 概率分布的概率生成模型。

= RAG (Retrieval-Augmented Generation，检索增强生成)
  == 定义（学理）： RAG 是一种混合架构和方法论，旨在通过整合外部知识库来增强 LLM 的生成能力。它将生成过程分为两个独立的、非微调的步骤：检索（Retrieval）和生成（Generation）。

  == 技术原理：

    - 索引： 将特定领域的私有文档（PDF,网页,数据库）切块、向量化（Embedding）并存储在向量数据库中。

    - 检索： 当用户提出查询（Query）时，系统将其转化为向量，在向量库中进行相似性搜索，找到最相关的K个文档片段。

    - 增强与生成： 将检索到的知识片段作为上下文（Context），与用户的查询一起作为提示（Prompt）输入给 LLM。LLM 利用这些事实进行推理和生成回答。

  == 功能： 解决了 LLM 的“幻觉”问题（不诚实地编造）、知识截止问题（无法回答最新知识），并允许模型在不重新训练的情况下访问企业或个人私有数据。

= Tool（工具）
  == 定义（学理）： 
  Tool 是一个被封装的外部功能单元，允许 LLM 通过生成结构化的调用指令（如 JSON），来执行特定的、非语言生成的物理操作或确定性计算任务。

  == 技术原理： 
  模型本身不能访问互联网或运行代码。Tool 提供了一个接口。当 LLM 在思考链（Thought Chain）中识别到需要执行外部操作（如查询实时天气、在计算器中计算大数、执行一段 Python 代码）时，它会输出一个 Tool Call 指令。系统执行器运行该 Tool，捕捉结果，并将结果作为“观察”（Observation）反馈给模型。

  == 功能： 
  Tool 将 LLM 从一个“文本生成器”扩展为一个“执行实体”，弥补了模型在确定性计算、实时信息获取、真实世界交互等方面的短板。

= Skill（技能）
  == 定义（学理）： 
  Skill 是 Agent 系统利用其内部逻辑（如思维链或反思循环）、结合可用的 Tool 或 RAG，以解决复杂、多步骤问题的特定方法或复合能力。

  == 技术原理： 
  Skill 是由 Agent 管理的一种预定义流程或高层方法。一个 Skill 可能是一个复杂的 Prompt工程（如“零样本思维链提示”），也可能是一个整合了 RAG 检索和多个 Tool 调用的自动化流（例如，一个“数据分析技能”会包括：从 RAG 检索数据字段 -> 调用 Python Tool 进行统计 -> 调用绘图 Tool 生成图表）。

  == 功能： 
  Skill 是 Agent 核心能力的具体体现。它允许将通用的 LLM 推理应用到特定的、高度结构化的任务领域。

= Agent (Autonomous Agent，智能体)
  == 定义（学理）： 
  Agent 是一个以 LLM 为核心决策组件的自主系统。它具备感知环境、建立目标、制定规划、利用工具、获取知识并根据反馈动态调整行动的封闭循环（Perception-Action Loop）。

  == 技术原理： 
  Agent 架构通常包含四个核心组件：

  - 核心推理（Logic Core）： 基于 LLM 的规划、决策、反思（如 CoT, ReAct 循环）。

  - 记忆（Memory）： 包括系统记忆（System Prompt）和用户上下文历史（长期/短期记忆）。

  - 规划与工具/技能执行： 决定使用哪些 Tools 或 Skills 来执行行动。

  - 感知（Perception）与反馈： 观察 Tool 或 RAG 的执行结果，作为输入反馈给推理核心进行下一步决策。

  == 功能： 
  Agent 是目前 AI 的最高形态。它不再是一个“对话机器人”，而是一个可以独立工作、规划任务、解决复杂工程或业务问题的“小管家”。

= MCP (Model Context Protocol，模型上下文协议)

  == 定义（学理）： 
  MCP 是一个新兴的、旨在标准化大型语言模型（Agent）与外部数据源、工具集和特定环境之间上下文交互、发现和执行机制的统一协议。

  == 技术原理： 
  类似于网络协议（如 HTTP），MCP 提供了一个标准化的接口规范。它允许外部系统（数据源、API 服务、本地文件系统）实现 MCP 接口，从而将自身动态地、安全地提供给多个不同的 Agent 调用。协议定义了如何定义工具的 Schema、如何安全地传输上下文（包括身份验证）、以及如何标准化地返回执行结果。

  == 功能： 
  解决了“工具孤岛”和“集成噩梦”问题。通过 MCP，一个 Agent 只需要实现一套 MCP 客户端代码，就可以连接任何遵循 MCP 协议的外部工具或知识库，极大地提高了多模型、多环境集成的互操作性。

\

#image("ai概念图.png")

