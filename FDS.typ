#set heading(numbering:"1.1")

#heading(numbering:none)[*FDS算法及其易错点整理*]

#show raw.where(block: true): it => {
  block(
    width: 100%,
    fill: luma(240),
    inset: 10pt,
    radius: 4pt,
    stroke: luma(200),
    it
  )
}
= 滑动窗口(e.g 爆气球问题)\

= 链表双指针/多指针法(e.g 单向链表翻转问题)\

== 多指针法是解决单链表问题的“万金油”，只要题目要求空间复杂度为 O(1)，大概率就要靠它。\

  它主要解决以下四类问题：

  - 链表翻转与重组：除了全局反转，还包括局部反转（反转区间 m 到 n）、每 K 个节点一组进行反转、将链表的前半段和反转后的后半段交替合并等。

  - 寻找特定位置的节点：例如寻找链表的中间节点、寻找链表的倒数第 K 个节点。这类问题通常演变为“快慢指针”（一个指针走得快，一个走得慢）。

  - 链表结构检测：最经典的就是判断链表是否包含环（同样使用快慢指针，看它们是否会在环内相遇），以及判断链表是否相交。

  - 链表属性判断：例如判断一个单链表是否为“回文链表”。通常的做法是：用快慢指针找到中点,反转后半段,前后两段同时遍历比对,把后半段反转复原。
== 算法的核心特点

- 极致的空间效率：不依赖数组、栈或哈希表等额外的数据结构，仅使用几个固定数量的指针变量，空间复杂度严格控制在 O(1)。

- 破坏性（就地修改）：它会直接改变原始链表的物理结构（指针指向）。在实际工程开发中，如果不希望原数据被破坏，通常需要先深度拷贝一份再操作；但在算法题中，这正是考点所在。

- 一次遍历（或有限次遍历）：时间复杂度通常为 O(N)。通过指针的巧妙配合，尽量避免对链表进行无意义的重复扫描。

== 标准用法与实战模板

掌握这种算法，其实就是熟练掌握一套标准的操作流程：

- 第一步：防御性编程与哑节点（Dummy Node）面对链表题，起手先判断 if (head == NULL || head->Next == NULL)。为了避免处理头结点的特殊情况，习惯性地在开头加上一个 Dummy 节点。

- 第二步：确定指针阵容反转类：通常需要 3 个指针（prev, curr, next）。寻找/检测类：通常需要 2 个指针（fast, slow），有时起步速度不同，有时起步位置不同。

- 第三步：确立循环边界思考循环什么时候停下？如果是遍历到底，通常是 while (curr != NULL)；如果是快慢指针寻找中点，通常是 while (fast != NULL && fast->Next != NULL)。

- 第四步：严格的“存、改、移”顺序进入循环体后，严格按照“先保存后继节点,修改当前节点指针,所有参与指针按顺序向后平移”的节奏写代码，绝不能打乱顺序。

== HW2编程题：
List Reverse( List L ) {
    // 如果链表为空或者只有一个实际节点，直接返回
    if (L == NULL || L->Next == NULL) {
        return L;
    }
    
    Position prev = NULL;
    Position curr = L->Next;
    Position next = NULL;
    
    // 开始遍历并反转
    while (curr != NULL) {
        next = curr->Next; // 1. 先保存下一个节点
        curr->Next = prev; // 2. 将当前节点的 next 指向前一个节点（反转）
        prev = curr;       // 3. prev 指针后移
        curr = next;       // 4. curr 指针后移
    }
    
    // 将带有 dummy header 的头结点连接到反转后的新链表头部（即 prev）
    L->Next = prev;
    
    return L;
}

= Stack & Queue（HW3)\
+  *算法思路 ：双栈模拟队列 (Two-Stack Queue)*

  队列是 FIFO（先进先出），栈是 LIFO（后进先出）。要用栈模拟队列，核心在于利用两次“后进先出”来抵消，达到“先进先出”的效果。
  - 角色分配：
    - 栈 $s_1$ (输入栈)： 负责接收新进入的元素（Enqueue）。
    - 栈 $s_2$ (输出栈)： 负责弹出最早进入的元素（Dequeue）。
  - 操作逻辑：
    - 入队 (I)： 直接压入 $s_1$。
    - 出队 (O)：
      - 如果 $s_2$ 不为空：直接弹出 $s_2$ 的栈顶。
      - 如果 $s_2$ 为空：把 $s_1$ 的所有元素逐个弹出并压入 $s_2$。此时，$s_1$ 的底部（最先进入的）变成了 $s_2$ 的顶部。
      - 如果两个栈都为空：报错 ERROR
+ *错误点复盘*：
#image("image.png")

= 数据结构知识点

== 栈
  - 栈就像一叠盘子，你最后放上去的盘子，必须最先拿出来。
  - 核心特性：LIFO (Last In, First Out)，即后进先出。
  - 基本操作：
  Push (入栈)：将元素压入栈顶。

  Pop (出栈)：从栈顶移除元素。

  Peek/Top (查看栈顶)：获取栈顶元素但不移除。
  - 性能：主要操作的时间复杂度均为 $O(1)$。
  - 常见应用场景：
    - 函数调用栈：编程语言处理递归和方法调用的基础。
    - 撤销操作 (Undo)：编辑器记录你的输入历史，按 Ctrl+Z 总是撤销最后一步。
    - 括号匹配：编译器检查代码中的 ()、[]、{} 是否成对闭合。
    - 表达式求值：将中缀表达式（如 $1 + 2$）转为后缀表达式进行计算。 

== 队列
  - 队列就像排队买票，先来的人先买到票离开。
  - 核心特性：FIFO (First In, First Out)，即先进先出。
  - 基本操作：
    - Enqueue (入队)：在队尾（Rear）添加元素。
    - Dequeue (出队)：在队头（Front）删除元素。
  - 性能：标准操作的时间复杂度均为 $O(1)$。- 常见应用场景：
    - 任务调度：打印机等待队列、操作系统中的进程调度。
    - 广度优先搜索 (BFS)：在图或树的遍历中，用来按层存储待访问节点。
    - 缓冲 (Buffering)：视频流播放时的预加载缓冲。

== 树
  + 核心术语 (Terminology)要把树学好，首先得认识它的“零件”。
    - 节点 (Node)：树中的每一个元素（比如图里的一个个圆圈）。
    - 根节点 (Root)：树的最顶端节点，一棵树只有一个根。
    - 叶子节点 (Leaf)：没有子节点的节点（树的最末端）。
    - 父节点 / 子节点 / 兄弟节点：表示节点间的层级关系。同一个父节点下的节点互为兄弟。
    - 深度 (Depth)：从根节点走到当前节点所需的步数（从上往下数）。
    - 高度 (Height)：从最底层的叶子节点走到当前节点所需的步数（从下往上数）。
  + 树的常见分类在考试或面试中，我们通常主要研究*二叉树（Binary Tree）*及其变种。
    - 普通二叉树 (Binary Tree)：每个节点最多只能有 2 个子节点（通常叫左子节点和右子节点）。
    - 满二叉树 (Full Binary Tree)：除了叶子节点外，每个节点都有两个子节点，且所有叶子都在同一层。
    - 完全二叉树 (Complete Binary Tree)：除了最后一层外，其他层都是满的，且最后一层的节点都尽量靠左排列（堆就是基于这个实现的）。
    - 二叉搜索树 (BST, Binary Search Tree)：自带排序属性的树。
      - 规则是：左子树的所有节点 < 根节点 < 右子树的所有节点。它的平均查找时间复杂度为 $O(log n)$。
    - 平衡二叉树 (AVL树 / 红黑树)：为了防止二叉搜索树在极端情况下“长歪”退化成链表（导致查询变成 $O(n)$），通过旋转操作保持树的左右高度平衡。
  + 核心操作：树的遍历 (Traversal)因为树不是直线的，所以怎么“逛遍”树的所有节点成了一个大学问。

    这里分为两大派系：
    
    - 派系一：深度优先搜索 (DFS) - 结合“栈”或“递归”实现这三种遍历的区别仅仅在于何时访问“根节点”：
      - 前序遍历 (Pre-order)：根 -> 左 -> 右。（常用于复制一棵树）
      - 中序遍历 (In-order)：左 -> 根 -> 右。（重点：二叉搜索树的中序遍历一定是一个递增的有序数组！）
      - 后序遍历 (Post-order)：左 -> 右 -> 根。（常用于删除整棵树，或者计算树的高度，因为必须先处理完子节点才能处理父节点）
    
    - 派系二：广度优先搜索 (BFS) - 结合“队列”实现层序遍历 (Level-order)：从上到下、从左到右，一层一层地访问。

